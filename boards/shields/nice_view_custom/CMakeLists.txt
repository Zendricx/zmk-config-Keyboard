if(CONFIG_ZMK_DISPLAY AND CONFIG_NICE_VIEW_WIDGET_STATUS)
  set(NICE_VIEW_BASE ${CMAKE_SOURCE_DIR}/boards/shields/nice_view)

  zephyr_library_include_directories(${CMAKE_SOURCE_DIR}/include)
  zephyr_library_sources(${NICE_VIEW_BASE}/custom_status_screen.c)
  zephyr_library_sources(${NICE_VIEW_BASE}/widgets/bolt.c)
  zephyr_library_sources(widgets/util.c)

  if(NOT CONFIG_ZMK_SPLIT OR CONFIG_ZMK_SPLIT_ROLE_CENTRAL)
    zephyr_library_sources(${NICE_VIEW_BASE}/widgets/status.c)
  else()
    zephyr_library_sources(${NICE_VIEW_BASE}/widgets/art.c)
    zephyr_library_sources(${NICE_VIEW_BASE}/widgets/peripheral_status.c)
  endif()
endif()
